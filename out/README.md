# Build output — .app packages

This folder is intentionally kept out of version control (see .gitignore).
Per the runbook: **never delete a previous package.** Every repackage writes a new,
uniquely-named file here.
