# Project Memory — Bootcamp Registration Tracking (BC PTE)

> Short anchor only. The *why* of each decision lives in `ChangeLog.md`; this file says
> *where to look* and *what is owed*.

## Current position

- **Phase:** BUILD → PROVE
- **Step:** 07 closed out (BUILD-21). Step 08 (Gap-Fit Test) run and fully resolved (STEP08-02). **Step 09 (Code Review) run and fully resolved 2026-09-13** — `docs/CodeReview.md` created, 11 findings, all applied (STEP09-02): 3 real defects fixed (BP-1 Amount Paid overwrite, BP-2 xRec staleness, BP-3 cue permission gap), SC-1 closed (Step 08's deferred G-04 — execute grants added), plus 6 minor fixes and 2 items scheduled to `Roadmap.md`. Recompiled clean, 20 files, 0/0. **Live verification still owed:** SC-1/BP-3's fixes need a non-SUPER-user sandbox test (Step 12 test case). Next: repackage (version bump proposed, awaiting AJ), then Step 10 (Update Design Documents).
- **Status:** **20 objects built (17 planned + 3 gap-fill). Full extension compiles 0 errors / 0 warnings** (20 files, CodeCop + UICop + PerTenantExtensionCop). B1–B5 as originally planned (BUILD-02..07); gap-fill BUILD-09 added Role Center Activity Cues (60842–60844). `app.json` version bumped `0.0.1.0` → `0.0.2.0` (BUILD-10) → `0.0.3.0` (BUILD-15) → `0.0.4.0` (BUILD-19), all directly by AJ. AJ retested the `0.0.2.0` package live and reported **neither** BUILD-12 nor BUILD-13 actually fixed anything. An Opus (Reasoning role) subagent re-diagnosed both from scratch; both original diagnoses were wrong and both are now corrected and independently verified against Microsoft Learn (BUILD-16). Package `Bootcamp_Registration_Tracking_0.0.4.0.app` built (BUILD-20) and published; **AJ confirmed both bugs fixed on a live retest 2026-09-13 (BUILD-21).** Step 08 Gap-Fit Test then run (STEP08-01) and resolved (STEP08-02): **G-12, a genuine, independently-verified code defect** — `ocpfBootcamp."Max Seats".OnValidate` recomputed `"Seats Remaining"` on a separate record copy *before* the page's own pending write committed, so the value went stale/wrong on any normal (non-wizard) bootcamp create or edit, contradicting FRD F-3/D-8 — **fixed**: now computed in memory directly on `Rec`. Version bumped to `0.0.5.0` (Revision — bug fix, no new features) and repackaged (BUILD-22): `outputAppPackage/Bootcamp_Registration_Tracking_0.0.5.0.app`, 20 files, 0/0, no schema changes since `0.0.4.0` (Force Sync not needed). Not yet published or retested live. Compile route: `alc.dll` from terminal against VS Code's pre-provisioned .NET 10 runtime (nothing installed).

## Live documents

| Document | Path | State |
|---|---|---|
| Problem Statement | `docs/ProblemStatement.md` | Signed off (PRE-01) |
| ChangeLog | `docs/ChangeLog.md` | Current through STEP09-02 |
| Project Memory | `docs/ProjectMemory.md` | This file |
| Gap Analysis (PRE-02) | `docs/GapAnalysis-PRE02.md` | Signed off |
| Gap Analysis (Step 08) | `docs/GapAnalysis.md` | 2026-09-13 — 20 findings, **all resolved** (STEP08-02) |
| Code Review (Step 09) | `docs/CodeReview.md` | 2026-09-13 — 11 findings, **all resolved** (STEP09-02); live verification of SC-1/BP-3 still owed |
| Roadmap | `docs/Roadmap.md` | **Updated 2026-09-13** — R-1 (no upgrade codeunit + new-company gap), R-2 (Confirm in transaction) |
| Project Parameters | `docs/ProjectParameters.md` | Confirmed by AJ 2026-09-10; §1.6/§1.7/§1.8 all now answered (§1.6/§1.8 backfilled at Step 08) |
| FRD | `docs/FRD.md` | Signed off; updated in place through Step 09 (F-3, F-8, D-8, D-9, §6.6 — STEP09-02) |
| TDD | `docs/TDD.md` | Signed off; updated through Step 09 (§4 items 3/7/12, §6.11, §6.16–18, §6.20 — STEP09-02) |
| Object Register | `docs/ObjectRegister.md` | **20 objects built** (17 planned + 3 gap-fill Activity Cues, BUILD-09); 60803 status corrected (G-19) |
| Sanity Check | `docs/SanityCheck.md` | Signed off |
| Build Plan | `docs/BuildPlan.md` | Batch order executed; `outputAppPackage/` references corrected (G-18) |
| Scaffold | `app.json`, `src/*`, `.gitignore`, git repo | Done — baseline `7e71b33`; Batches 1–5 on top (`ea341d2`…latest) on branch `build/bootcamp-registration` |
| Requirements (pre-BUILD source) | `requirements/bootcamp-registration-extension-requirements.md` | Given input, frozen |

## Open decisions

| # | Decision | Awaiting |
|---|---|---|
| 1 | Confirm whether `.vscode/mcp.json` actually connects the AL MCP server in this Claude Code host (may need a reload) | awaiting: AJ |
| 2 | **Repackage and version bump** — Step 09's fixes (BP-1, BP-2, BP-3, BP-7, SC-1, + minors) are applied and recompiled clean but not yet packaged/redeployed; `outputAppPackage/…0.0.5.0.app` predates all of them | awaiting: AJ |
| 3 | **Live, non-SUPER-user sandbox test of SC-1/BP-3's fix** — permission execute grants + cuegroup visibility guard are applied but unverified live (Step 12 test case) | awaiting: AJ |
*(Resolved 2026-09-13: Bug 2's discriminating test / retest question, and publishing `0.0.4.0` — both closed by BUILD-21. Step 08 Gap-Fit Test run and fully resolved — STEP08-01/STEP08-02. Step 09 Code Review run and fully resolved — STEP09-01/STEP09-02, see `ChangeLog.md` and `docs/CodeReview.md`. `ajansari/ocpfBCALPatterns`'s existence — AJ confirmed directly, independently re-verified via `git ls-remote` (public, `main` branch, no auth needed) and its rendered GitHub README, which matches this project's own local `patterns/README.md`. `CLAUDE.md`'s "public" claim restored with the verification method and date attached.)*

## Milestone log

- 2026-09-10 — Project kicked off from `requirements/bootcamp-registration-extension-requirements.md`. Four clarifying decisions taken (see `ChangeLog.md` Issue DEFINE-01).
- 2026-09-10 — PRE-01 signed off by AJ.
- 2026-09-10 — PRE-02 gap analysis signed off: no ledger/posted/tax tables (descriptive payment only); No. Series (T308) + Customer (T18) added as references; Location/Topic stay free text; 5 items deferred with reasoning.
- 2026-09-10 — Step 01 intake: identity = Bootcamp Registration Tracking / OnlyCopilotFans / SaaS PTE / ns OCPF.BootcampRegistration / loc NA / prefix ocpf; ID range 60800–60899; permission sets = Yes. See `ChangeLog.md` Issue DEFINE-02.
- 2026-09-10 — DEFINE complete; params confirmed. FRD drafted (F-1…F-15, D-1…D-15). 8 open questions resolved by AJ — see `ChangeLog.md` Issue DESIGN-01.
- 2026-09-10 — FRD signed off. TDD drafted: 17 objects, IDs 60800–60891, 5 batches. Seats Remaining changed from FlowField to subscriber-maintained stored field (ChangeLog DESIGN-02).
- 2026-09-10 — TDD signed off. Sanity Check run: batch plan restructured (core tables ship in one batch — mutual FlowField/TableRelation reference, ChangeLog DESIGN-04); `addlast(sections)` fixed; Customer-delete dangling link accepted.
- 2026-09-10 — Sanity signed off. Step 05: scaffold + git baseline (7e71b33). Compile route = Option A. Step 06: Batch 1 (Foundation) generated, pre-flight clean, handed to AJ for compile.
- 2026-09-10 — Pre-release version scheme: `app.json` `version` set to `0.0.1.0`; increment through the test cycle, roll to `1.0.0.0` at go-live (ChangeLog BUILD-01).
- 2026-09-10 — Batch 1 (Foundation) compiles 0/0. PTE0004 fixed by moving permission sets to Batch 1, scoped to tables built so far and grown per batch (ChangeLog BUILD-02); 4 Foundation files renamed to `ocpf*` (AA0215). Terminal compile via VS Code's provisioned .NET runtime. Commit `ea341d2`.
- 2026-09-10 — Batch 2 (core tables + Mgt codeunit) compiles 0/0. AJ approved 2 TDD deviations (BUILD-04): LookupPageId/DrillDownPageId deferred to Batch 3; `Max Seats <= 0` = no overbooking cap. Lint fixes: AA0244 (shared `Bootcamp` var → locals), AA0240 (email label wording). Commit `6c970e5`.
- 2026-09-10 — Batch 3 (in-client pages) compiles 0/0 (BUILD-05). 4 pages + LookupPageId/DrillDownPageId added to both tables. Card gets `UsageCategory = None` (silences info AW0006). Commit `c6e0bb3`.
- 2026-09-10 — Batch 4 (API pages) compiles 0/0. AJ decided to follow CodeCop AA0101 over the Standards §1.3 literal example: `APIPublisher`/`APIGroup` fully camelCased, dropping the `ocpf_` separator (ChangeLog BUILD-06) — a per-project divergence, not a Standards amendment. Commit `0d12f41`.
- 2026-09-10 — Batch 5 (Wizard, RC pageextension, Install revisit) compiles 0/0 (BUILD-07). **All 17 objects built; full extension 0 errors / 0 warnings.** Caught and fixed a real staleness bug in `CreateSampleBootcamps` before it ever ran (Insert-then-Validate would have persisted `Seats Remaining = 0`) by setting fields before the single `Insert(true)`. BUILD phase (Step 06) done. Commit `5cdd441`.
- 2026-09-10 — First package built: `out/Bootcamp_Registration_Tracking_0.0.1.0.app` (BUILD-08). Not published to any sandbox yet (AJ's choice). Next: publish + Step 09 green/red-team testing when AJ is ready, or Step 07/08 doc reconciliation.
- 2026-09-11 — CLAUDE.md (runbook) updated per AJ's feedback: compile-once cadence (Operating Rule 4), tooling-install rule (6b), new intake §1.6 Onboarding & Discoverability + §1.7 Model/Effort Preference, permission-set coverage enforced at every step (Standards §7.3 cross-refs added to Steps 03/04/05/06/10).
- 2026-09-12 — Gap-fill: Role Center Activity Cues added (ChangeLog BUILD-09) — 5 cues (Active Bootcamps, Unpaid Registrations, Below Min Seats, Registrations This Month, Bootcamp Revenue This Month) via tableextension 60842 + codeunit 60843 + pageextension 60844. 20 files, 0/0. No permission-set change needed (tableextension on a standard table, not a new one).
- 2026-09-12 — Version bumped `0.0.1.0` → `0.0.2.0` for BUILD-09 (ChangeLog BUILD-10). AJ overrode the Minor recommendation deliberately — pre-1.0 test cycle treated as flat sequential builds.
- 2026-09-12 — First manual testing round (`TestingFeedback.md`): 2 findings. Attendee line getting blank Bootcamp No. = real bug, fixed via a defensive `OnNewRecord` on `ocpfAttendeeSubform` (BUILD-12). Sample-bootcamp "already exists" was first misdiagnosed as stale data (BUILD-11) — AJ disproved that with an empty table + a brand-new series reproducing it — corrected diagnosis: both sample inserts computed the same number, second collided, error rolled back both (looks like empty table + "already exists" together). Fixed with self-healing number assignment in `ocpfBootcampRegMgt` (BUILD-13). 20 files, 0/0.
- 2026-09-12 — Package built at `0.0.2.0` (BUILD-14), includes BUILD-09..13. Prior `0.0.1.0` package left untouched in `out/`. Not yet published/retested live.
- 2026-09-12 — Runbook updated: fixed three-role work division (main/light/reasoning), model-agnostic, superseding the earlier "record preference only" default. Wired into Steps 02/03/05/06/07/10 and Testing Feedback Log.
- 2026-09-12 — `app.json` version bumped directly by AJ to `0.0.3.0` (BUILD-15) — no package built at this version yet.
- 2026-09-12 — Runbook (still v2.0.0.0, folded in — AJ's call not to bump until told): added AL MCP Server + BCQuality Knowledge Snapshot support (RunbookChangelog.md). Executed for this project: `.bcquality/` snapshot fetched (commit `35d0966a`, 806 files, tracked in git alongside `.alpackages/`), `scripts/al-mcp-server.sh` (portable wrapper, re-discovers extension/runtime paths at launch) + `.vscode/mcp.json` written. Live-verified the MCP server's real tool list (16 tools) directly via a JSON-RPC handshake — differs from the brief that proposed this (no `al_debug`; several tools it didn't mention). Not yet confirmed connected as a live capability inside any Claude Code session — `claude mcp add` isn't reachable from this session's Bash tool (native VS Code extension host, no CLI on PATH); AJ to confirm whether `.vscode/mcp.json` gets picked up after a reload.
- 2026-09-12 — §1.7 Model & Effort Assignment configured for this project (`ProjectParameters.md` §1.7): Main = Sonnet, Light = Haiku, Reasoning = Opus, via the `Agent` tool. Applies going forward; steps already done were single-model and aren't redone.
- 2026-09-12 — Operating Rule 4 revised again (AJ Ansari): compiling is no longer an automatic part of BUILD at all — per-batch pre-flight now includes symbol verification (every standard-object reference confirmed against the downloaded symbol source), and the compiler is invoked only when AJ explicitly asks. This project's own 5 build batches + gap-fill were already compiled per-batch under the older rule (nothing to redo — code is already compiled clean); applies to any future gap-fill work.
- 2026-09-12 — AJ retested the live `0.0.2.0` package: neither BUILD-12 nor BUILD-13 actually resolved anything. Spun up an Opus (Reasoning role) subagent per §1.7 to re-diagnose both from scratch without editing code; both diagnoses independently verified against Microsoft Learn before trusting them (ChangeLog BUILD-16). Bug 1's real cause: `Bootcamp.Init()` reused across two inserts doesn't clear the primary key, so the second insert skips numbering and collides — fixed with an explicit `Bootcamp."No." := '';` after each `Init()`; BUILD-13's self-healing retry loop removed as unnecessary (AJ's call). Bug 2's real cause: `SubPageLink` filters live in filter group 4 ("Link"), not the default group 0 that BUILD-12's plain `GetFilter()` read — fixed with a `FilterGroup(4)` read (restored after), a hidden `Bootcamp No.` field control on the subform, and a `TestField` guard on `ocpfAttendee.OnInsert` to fail loudly instead of silently saving a blank key. BUILD-12 and BUILD-13's original ChangeLog entries marked superseded, not deleted. Also found and fixed, same pass: `.bcquality/` nested inside the project root was breaking compilation (470+ errors, all inside non-compilable `.good.al`/`.bad.al` snippet files) — relocated to a sibling directory outside the project root; `.gitignore` and `CLAUDE.md` updated to reflect the new location. Recompiled clean: 20 files, 0 errors / 0 warnings. TDD updated (§6.5, §6.11, §6.14).
- 2026-09-12 — Package built at `0.0.3.0` (BUILD-17), includes BUILD-16. Prior `0.0.1.0`/`0.0.2.0` packages left untouched in `out/`. Not yet published/retested live — see Open decisions #2 and #3.
- 2026-09-12 — Runbook updated (BUILD-18): output-folder name fixed as `outputAppPackage/` (stated at intake and at every completed build), plus a new requirement to flag Schema Sync Mode/Force Sync need on every completed build, verified against current Microsoft Learn docs. Applied to this project: `out/` renamed to `outputAppPackage/` (all three prior packages preserved, none deleted); `.gitignore` updated. BUILD-17's `0.0.3.0` package assessed retroactively: no schema changes, safe under the default Add sync mode.
- 2026-09-12 — Runbook version bumped to **v2.0.1.0** (AJ Ansari) — the BUILD-18 output-folder/Force Sync changes moved out of the v2.0.0.0 baseline entry into their own `RunbookChangelog.md` v2.0.1.0 section, since v2.0.0.0 was already established as the first versioned baseline before this change happened.
- 2026-09-12 — `RunbookSchematics.md` §4.2 diagram updated (folded into v2.0.1.0): Light/Reasoning role boxes now say "runs in a subagent" (Main says "runs in the primary agent session"), and each role box got an example recommended model (Sonnet/Haiku/Opus for Main/Light/Reasoning) matching this project's own §1.7 configuration.
- 2026-09-12 — `app.json` version bumped directly by AJ to `0.0.4.0` (BUILD-19), discovered via `git status`, same pattern as BUILD-15. AJ confirmed it and asked for the build (BUILD-20): package built at `0.0.4.0`, 20 files, 0/0, no schema changes since `0.0.2.0` (Force Sync not needed). Not yet published/retested live — see Open decisions #3.
- 2026-09-13 — AJ published `0.0.4.0` and retested live: **"Good news - everything tested well."** Both bugs from the original testing round (sample-bootcamp collision, blank Attendee Bootcamp No.) confirmed fixed — BUILD-16's corrected diagnoses hold (BUILD-21). Closes the open discriminating-test and publish/retest items.
- 2026-09-13 — AJ chose Step 08 (Gap-Fit Test) as the next PROVE step. An Opus reasoning-role subagent ran a formal FRD vs. TDD vs. as-built three-way comparison (`docs/GapAnalysis.md`, STEP08-01): 20 findings (G-01…G-20), mostly small documentation-integrity items, plus **one real, independently-verified code defect (G-12)** — `ocpfBootcamp."Max Seats".OnValidate` recomputes `"Seats Remaining"` on a stale, separate record copy that gets overwritten by the page's own pending write, so the field goes wrong on any normal bootcamp create/edit (contradicts FRD F-3/D-8; masked until now because BUILD-07 had already worked around the same bug shape inside the wizard specifically). Main role independently re-traced BC's own field-validation/insert-order semantics against the actual code before trusting the subagent's claim.
- 2026-09-13 — AJ approved "Fix everything now" (STEP08-02). Applied: **G-12** (Seats Remaining now computed in memory on `Rec`, no more stale re-`Get()`), **G-13** (5 ToolTips on the Activity Cue gap-fill), **G-03** (Setup card `UsageCategory = Administration`, now Tell Me-findable) — recompiled clean, 20 files, 0/0. Applied all documentation-only corrections: FRD gained F-16 (Activity Cues), the "0 = no cap" semantic (F-10/§6.1), a D-5 scope fix, and a §6.6 permission-grant reword; TDD gained §6.18–§6.20 (the 3 gap-fill objects), a corrected §6.3 rule (with a generalizable "never re-Get() your own record inside its own OnValidate" note), a corrected §6.14 (actual wizard navigation) and §6.15 (`Image` name); `ProjectParameters.md` §1.3 corrected to the shipped API identity, with §1.6/§1.8 backfilled and the exit-gate corrected; `BuildPlan.md`/`ObjectRegister.md` stale notes fixed; `docs/Roadmap.md` created (R-1: no upgrade codeunit, scheduled). Deferred to Step 09 as named test cases (not silent assumptions): G-14 (cue read-permission — the one item with real blast radius if confirmed), G-04 (page-execution permissions), G-05 (uninstall orphan), G-03's Tell Me visibility. Extension now ready for Step 10 (Code Review).
- 2026-09-13 — AJ approved a version bump + repackage (BUILD-22): `app.json` `0.0.4.0` → `0.0.5.0` (Revision — bug fix, no new features). Recompiled clean, built `outputAppPackage/Bootcamp_Registration_Tracking_0.0.5.0.app`. No schema changes since `0.0.4.0`. Not yet published/retested live.
- 2026-09-13 — **Major runbook restructure** (framework-level, `CLAUDE.md`/`RunbookSchematics.md`/`RunbookChangelog.md` — gitignored per §1.8, not committed to this project's repo). AJ: "Step 9 keeps tripping me up." Old Step 09 ("Package and Test the App") removed — it tested against a `HumanUnitTestScript.md` that, in the old sequence, hadn't been written yet (that script was a Step 12 output). Operating Rule 4 and Step 07 changed from "compile" to "compile and package," made a continuous cycle (publish → test → diagnose → fix → repackage → repeat), not a one-time event; the same cycle now applies to Step 08's gap-fixes and Step 09's Code Review fixes too. A new, optional, agent-run API test pass (gated on a live MCP connection, else recommends Postman/Power Automate/Power Apps/Copilot Studio) was added at the end of Step 07. Code Review/Update Design Docs/Document the Code renumbered 09/10/11 (was 10/11/12). Step 11 (Document the Code) now asks the human whether they also want Automated Test Scripts created, alongside the Human Unit Test Script. New Step 12, "Release to Users for Testing," added — the human-run counterpart to Step 07's optional pass, run after Code Review and documentation exist; its exit gate is also the production go-live gate (the tested package ships as-is, no rebuild). Net step count: still 12, reshuffled. Doesn't affect this project's own completed steps (01–08 are unaffected by the renumbering since they kept their numbers); relevant going forward once this project resumes Step 09 (Code Review).
- 2026-09-13 — Independent Opus reasoning-role review of the restructure above found 21 findings; main role independently re-verified the 4 highest-priority ones before trusting them, then applied all 21 (per §1.7, reasoning role reports, main role fixes). Real defects: a package-overwrite/never-delete policy collision under the new "repackage every fix" default (AJ's call: never-delete protects across versions, not within one — the package that passes Step 12 gets its Build segment bumped before shipping); a stale "Code Review (10)" in `RunbookSchematics.md` §4.2; three residual contradictions of the old one-time-compile model; a PROVE diagram gate drawn before the fix-loop that satisfies it. Runbook version bumped to **v2.1.0.0** (AJ's call — this and the restructure together were too large to fold into the already-shipped v2.0.1.0). No project files touched — all framework-level, gitignored per §1.8.
- 2026-09-13 — `patterns/` created (Opus subagent): two AL pattern write-ups (`Pattern-SubPageLink-FilterGroup4.md`, `Pattern-Init-Does-Not-Clear-Primary-Key.md`) extracted from this project's own BUILD-12/13/16 bug history, plus a `README.md` — "OnlyCopilotFans Business Central AL Patterns," created by AJ Ansari, meant to be a standalone, reusable project of its own (GitHub repo `ajansari/ocpfBCALPatterns` — existence/visibility not yet confirmed, see Open decisions). Runbook updated (folded into v2.1.0.0, no further version bump) with a new ALL ALONG section, "OCPF BC AL Patterns Library" — bootstraps this library into every future project's `patterns/` folder at Step 05 (alongside AL MCP Server + BCQuality), wired into Step 07 troubleshooting and the Testing Feedback Log (check patterns/ before diagnosing from scratch), a contribution hook at Step 09 Code Review, always gitignored (same class as BCQuality), with a human-specified merge rule (append to an existing `README.md` rather than overwrite it, under a fixed delimiter for refresh idempotency). This project's own `patterns/` folder is treated as already satisfying that bootstrap step — no fetch was actually run here.
- 2026-09-13 — Limited-scope Opus review of the Patterns Library addition above found 10 findings; main role verified the top ones, then applied all 10. Real issues: a refresh-idempotency bug (appending the upstream README a second time on every refresh, now fixed with a delimiter convention); a flat "public repo" claim in `CLAUDE.md` inconsistent with this file's own "meant to be" hedge (resolved by dropping the unverifiable claim and adding an "if unreachable, continue" fallback — see Open decisions); a stale pre-restructure Step 07 title reference; the collision rule not marked as agent-filled or citing Operating Rule 6a; the Testing Feedback Log claiming a check that wasn't actually wired there. Also fixed, found by the same review but unrelated to this feature: `RunbookChangelog.md`'s version sections weren't in newest-first order — reordered.
- 2026-09-13 — AJ confirmed `ajansari/ocpfBCALPatterns` exists; independently re-verified rather than taken on his word alone — `git ls-remote` confirmed it's public and reachable with a `main` branch, and its rendered GitHub README matches this project's own local `patterns/README.md` exactly (same title, same opening paragraph, same two pattern files). `CLAUDE.md`'s "public" claim restored with the verification method and date attached; Open decision closed.
- 2026-09-13 — AJ published `0.0.5.0` to sandbox; said to move on to Step 09 and beyond. An Opus reasoning-role subagent ran Step 09 (Code Review) across all 20 objects — `docs/CodeReview.md` created (STEP09-01): 11 substantive findings. **Three real defects, independently re-verified by main role, one against real Microsoft AL platform behavior (BP-2's `xRec` gotcha, confirmed via multiple independent sources since Microsoft's own reference page doesn't spell it out):** BP-1 (`Amount Paid` silently re-billed over a deliberate `0`, contradicting the shipped ToolTip/FRD/TDD — 3 resolution shapes proposed, needs AJ's decision), BP-2 (`xRec` dead on code/API-driven modifies, leaving the *old* bootcamp's Seats Remaining stale on a reassignment — same F-3/D-8 contract G-12 protected, different trigger), BP-3 (G-14's proposed cue-permission guard would be insufficient — 2 of 5 cues are FlowFields the platform calculates outside the guarded codeunit). **SC-1 confirms Step 08's deferred G-04 is still unresolved** — permission sets grant `tabledata` only, no execute grants, a genuine Standards §7.3 gap. 8 minor findings (redundant/drifted ToolTips, missing `ShowMandatory`/`DataClassification`, a performance nit, stale `ObjectRegister.md` rows). Both existing OCPF BC AL Patterns fixes re-verified as still correctly applied; three new pattern candidates flagged, not added unilaterally. Awaiting AJ's decisions on BP-1's resolution shape and the SC-1/BP-3/G-04/G-14 test-first-vs-fix-blind question.
- 2026-09-13 — AJ chose BP-1 resolution (a) and "fix now, verify after" for SC-1/BP-3 (STEP09-02). Applied: **BP-1** (SeedAmountPaid now called only from `"Bootcamp No." OnValidate`; required also fixing `ocpfAttendeeSubform.OnNewRecord` to use `Rec.Validate` instead of a plain assignment, or the normal subform flow would have stopped seeding Amount Paid entirely — verified the API page's field order still lets an explicit `amountPaid: 0` win); **BP-2** (new `OnBeforeModifyEvent` subscriber does `xRec.Get(xRec."No.")`, the documented fix for `xRec` not being a reliable before-image on code/API-driven modifies — verified against multiple independent sources since Microsoft's own reference page doesn't spell it out); **SC-1/BP-3** (added `page … = X` execute grants on all 8 own pages to the Read set; added an `OnOpenPage`-computed `Bootcamp.ReadPermission()` guard on the Activity Cues `cuegroup`'s `Visible`, covering the 2 FlowField cues a codeunit-only guard would have missed); **BP-7** (`Seats Remaining` clamped to 0 when Max Seats ≤ 0, refactored into one shared `CalcSeatsRemaining()` procedure). Plus CQ-2, BP-4, BP-5, CQ-3, PERF-1, R-1 (40 redundant ToolTips removed across 5 files), and the `ObjectRegister.md` correction. R-2 checked and found not to apply (no such `using` line existed — verified before acting). BP-6/BP-8 scheduled to `Roadmap.md`. Recompiled clean after every batch: 20 files, 0/0. TDD and FRD updated to match. **Still owed:** repackage/redeploy, and a live non-SUPER-user test of SC-1/BP-3's fix.
